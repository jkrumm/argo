# Argo — Project Configuration

## Workspace Layout

Workspaces: `apps/*` + `packages/*`

- `apps/api` — Elysia + Bun backend (`@argo/api`)
- `apps/dashboard` — Vite + React 19 + Mantine v9 frontend (scaffolded in Group 3)
- `packages/charts` — Theme-agnostic visx primitives (`@argo/charts`, scaffolded in Group 5)
- `packages/dashboard` — Legacy Refine v5 + AntD dashboard; kept until Group 11 cutover
